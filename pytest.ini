[pytest]
testpaths = tests
addopts =
    -v
    --strict-markers
    --tb=short
    --browser=chromium
    --alluredir=allure-results
    --clean-alluredir
markers =
    smoke: критичный путь, гоняется на каждый коммит
    regression: полный набор
    defect: воспроизводит известный дефект стенда
